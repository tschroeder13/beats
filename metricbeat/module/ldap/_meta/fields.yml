- key: ldap
  title: "LDAP"
  release: beta
  description: >
    LDAP data and metrics collection
  fields:
    - name: ldap
      type: group
      description: >
        `ldap` contains metrics and data that were obtained from LDAP searches
      fields:
