////
This file is generated! See scripts/mage/docs_collector.go
////

:modulename: ldap
:edit_url: https://github.com/elastic/beats/edit/main/metricbeat/module/ldap/_meta/docs.asciidoc


[[metricbeat-module-ldap]]
== ldap module

beta[]

This is the ldap module.



:edit_url:

[float]
=== Example configuration

The ldap module supports the standard configuration options that are described
in <<configuration-metricbeat>>. Here is an example configuration:

[source,yaml]
----
metricbeat.modules:
# Module: ldap
# Docs: https://www.elastic.co/guide/en/beats/metricbeat/8.6/metricbeat-module-ldap.html

- module: ldap
  metricsets: ["ldapsearch"]
  enabled: true
  period: 10s
  # searches will be carried out against these hosts
  hosts: ["localhost"]
  # # consider using secrets keystore as described here:
  # # https://www.elastic.co/guide/en/beats/metricbeat/current/keystore.html
  # bind_dn: "${LDAP_USER}"
  # bind_pw: "${LDAP_PWD}"

  # # Optional SSL.
  # # for further information see https://www.elastic.co/guide/en/beats/metricbeat/current/configuration-ssl.html
  # # By default is off.
  #ssl.enabled: true
  # ssl.certificate_authorities: ["../some/relative/path"]
  # ssl.certificate: ../some/relative/path
  # ssl.key: ../some/relative/path

  # # configure 'ldapsearch' metricset's searches.
  #ldapsearch.searches:
  # # Every search needs an unique namespace,
  # # which is later used for identification
  #  - name: "some unique name"
  # # host portion of url is *NOT* used (see "hosts")
  #    url: "ldaps://your.ldap.host:636/cn=search,o=base?some,return,attributes,list?sub?(objectClass=inetOrgPerson)#never#10#60#false"
  # # mode options:
  # #     - count: returns entry count
  # #     - requested: returns requested attributes from url for all result entries
  # #     - all: returns all attributes (user and operational) for all result entries
  #    mode: "count"

----

[float]
=== Metricsets

The following metricsets are available:

* <<metricbeat-metricset-ldap-ldapsearch,ldapsearch>>

include::ldap/ldapsearch.asciidoc[]

:edit_url!:
